HTTP 메서드 종류
1. GET : 리소스 조회
	- 전달받은 쿼리파라미터로 리소스 조회'
	- GET 메서드로 메시지 바디에 데이터를 전달할 수 있으나 권장하지 않음(지원하지 않는 서버가 많기 때문)
	- 캐싱 가능(조회에 사용하는 이유)

2. POST : 요청 데이터 처리(주로 등록)
	- 메시지 바디를 통해 전달받은 데이터로 신규 리소스를 생성하거나, 변경, 프로세스 처리 등에 사용
	- 모든 메서드는 POST로 대체 가능

3. PUT : 리소스가 있으면 완전 대체, 없으면 생성
	- 클라이언트가 리소스 위치를 알고 URI 지정(POST와 차이점)

4. PATCH : 리소스 부분 변경
5. DELETE : 리소스 삭제

HTTP 메서드 속성
1. 안전(Safe Methods)
	- 호출해도 리소스를 변경하지 않는다.(GET, HEAD)

2. 멱등(Idempotent Methods)
	- 몇번 호출하든 결과가 동일(GET, PUT, DELETE)
		* 외부요인으로 인한 리소스 변경은 고려하지 않음
	- 활용 : 자동 복구 매커니즘(서버가 정상응답을 주지 못했을 때, 클라이언트가 같은 요청을 해도 되는지에 대한 판단근거)

3. 캐시가능(Cacheable Methods)
	- 응답 결과를 캐시해서 사용해되 되는지(GET, HEAD, POST, PATCH)
		* 주로 GET, HEAD만 사용